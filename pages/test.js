let main = document.querySelector(".content");

const newQuestion = (indice) => {
    // NUMERACION
    // PREGUNTA
    // puntos
    // RESPUESTA 1 = Correcta
    // RESPUESTA 2
    // RESPUESTA 3
    // RESPUESTA 4
    let quest = document.createElement("h2");
    let 
    //indice.appendChild();
};
let random = Math.floor(Math.random() * 6) + 1
document.body.innerHTML = random;